spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coursemanagement?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieve=true
    username: juanda_dev
    password: juanda1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true